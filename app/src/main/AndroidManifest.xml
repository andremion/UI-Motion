<?xml version="1.0" encoding="utf-8"?>
<manifest
    package="com.example.andremion.transitions"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.example.andremion.transitions.MainActivity"
            android:theme="@style/AppTheme.Main">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name="com.example.andremion.transitions.DetailActivity"
            android:theme="@style/AppTheme.Detail"/>
        <activity
            android:name="com.example.andremion.transitions.SharingActivity"
            android:theme="@style/AppTheme.Sharing"/>
    </application>

</manifest>
